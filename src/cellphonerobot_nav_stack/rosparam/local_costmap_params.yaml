local_costmap:
  global_frame: odom
  robot_base_frame: base_link
  update_frequency: 1.8
  #publish_frequency: 0.5
  static_map: false # change to true if using an existing map or a map server
  rolling_window: true
  width: 2.0
  height: 2.0
  resolution: 0.1
